[project]
name = "sniff-collector"
version = "0.1.0"
description = "FastAPI receiver and PostgreSQL collector for MACrotrack sniff batches"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "psycopg[binary]>=3.2.13",
    "pydantic-settings>=2.12.0",
    "sqlalchemy>=2.0.44",
]
